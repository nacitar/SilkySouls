// 

namespace SilkySouls.Enums;

public enum State
{
    EarlyAttached,
    Attached,
    Detached,
    Loaded,
    FadedIn,
    NotLoaded,
    FirstLoaded,
    OnNewGameStart,
    AppClosing,
    AppStart,
    EventTabActivated
}